<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools"
          package="com.krunal.kotlincoroutine">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme"
            android:usesCleartextTraffic="true"
            tools:ignore="GoogleAppIndexingWarning" tools:targetApi="m">

        <activity android:name=".ui.MainActivity"
                  android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".ui.ThreadsActivity"
                  android:label="@string/title_threads"/>

        <activity android:name=".ui.CallbacksActivity"
                  android:label="@string/title_callbacks"/>

        <activity android:name=".ui.PromiseActivity"
                  android:label="@string/title_promise"/>

        <activity android:name=".ui.SuspendActivity"
                  android:label="@string/title_suspend"/>

        <activity android:name=".ui.AsyncActivity"
                  android:label="@string/title_suspend"/>

        <activity android:name=".ui.FutureActivity"
                  android:label="@string/title_future"/>
    </application>

</manifest>